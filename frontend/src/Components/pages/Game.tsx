import React from 'react';
import { useParams } from 'react-router-dom';
import {link} from 'react-router-dom';
import { CyberpunkLayout } from '../../Constant/CyberpunkLayout.tsx';

const Game: React.FC = () => {
return (
    <CyberpunkLayout isLoading={isLoanding}>
        
    </CyberpunkLayout>
)
}
